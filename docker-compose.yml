services:
  app:
    image: ghcr.io/cbdevelopment/note-scan:latest
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3010:3000"
    env_file: .env
    volumes:
      - notescan-data:/data

volumes:
  notescan-data:
